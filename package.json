{
  "name": "storybook",
  "private": true,
  "version": "2.3.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --noEmit 2>/dev/null || tsc -b",
    "preview": "vite preview"
  },
  "dependencies": {
    "jspdf": "^2.5.2",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
